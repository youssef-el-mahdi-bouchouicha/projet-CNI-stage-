<form method="post" action="{{ path('client_delete', {'idc': client.idc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.idc) }}">
    <button style="width: auto" class="btn btn-block btn-outline-danger">Supprimer</button>
