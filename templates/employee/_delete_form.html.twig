<form method="post" action="{{ path('employee_delete', {'ide': employee.ide}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input  type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee.ide) }}">
    <button style="width: auto" class="btn btn-block btn-outline-danger">Supprimer</button>
</form>
