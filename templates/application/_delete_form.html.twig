<form method="post" action="{{ path('application_delete', {'idapp': application.idapp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ application.idapp) }}">
    <button style="width: auto" class="btn btn-block btn-outline-danger">Supprimer</button>
</form>
